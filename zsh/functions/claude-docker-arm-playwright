#!/usr/bin/env zsh
# Run Claude Code in the native linux/arm64 browser-capable (Playwright) container.
# The base arm64 image must already be built. Extra args pass through to claude.
_claude_docker_run arm64 playwright "$@"
